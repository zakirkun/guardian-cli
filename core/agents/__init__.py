"""Sub-package: specialised agents beyond the four core ones.

Track A2 introduces three debate roles (red/blue/judge). Each is a thin
``BaseAgent`` subclass with its own system prompt; orchestration lives in
``debate_triage.DebateTriage``.
"""
